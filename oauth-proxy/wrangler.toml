name = "decap-oauth"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets are NOT set here — run these once after `npm i -g wrangler` and `wrangler login`:
#   wrangler secret put GITHUB_CLIENT_ID
#   wrangler secret put GITHUB_CLIENT_SECRET
# Then deploy with:
#   wrangler deploy
