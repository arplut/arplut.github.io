// Stub used by vite.config.ts when src/admin/ is absent (CI/CD, fresh clones).
// Admin pages are gitignored and local-only; this null component takes their place.
export default function AdminStub() { return null; }
